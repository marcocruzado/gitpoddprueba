<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<title>marco con gitpod</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/4.3.1/litera/bootstrap.min.css">
</head>

<body>
	<div class="container p-5">
		<div class="row">
			<div class="col-md-4 offset-md-4">
				<form action="/crear" method="POST" class="card card-body">
					<div class="form-group">
						<input type="text" name="tarea" placeholder="escribe una tarea a realizar" class="form-control" autofocus>
                    </div>
                    <div class="form-group">
						<input type="text" name="descripcion" placeholder="de que se trata tu tarea" class="form-control">
                    </div>
                    <button class="btn btn-primary">
                        Guardar
                    </button>
				</form>

                <% tareas.forEach(funtion(tarea){%>
                       <div class="card card-body mt-2">
                        <h5><%= tarea.tarea%></h5>
                        <p><%= tarea.descripcion%></p>
                       </div>
                <%})%>


			</div>
		</div>
	</div>
</body>

</html>